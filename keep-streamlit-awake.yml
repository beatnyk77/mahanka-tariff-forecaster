name: Keep Streamlit Awake

on:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          curl -s https://mahanka-tariff-forecaster-3zxxp8fmqldf6gu5pzs3uw.streamlit.app/ > /dev/null 2>&1 || true
